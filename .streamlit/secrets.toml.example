# Copy to .streamlit/secrets.toml for LOCAL use (never commit the real file),
# or paste into Community Cloud -> app -> Advanced settings -> Secrets.
WOS_API_KEY = "paste-your-web-of-science-expanded-api-key"
